# Валидация данных vet-insilico (InSilico Hub)

**Дата:** 2026-09-17 · **Клон:** `D:/vet-insilico` (commit `fbfb293`)
**Метод:** извлечение TS-данных через Bun 1.3.14 → валидация химии через RDKit 2026.03.6 (managed venv).

> **Статус правок:**
> - **2026-09-17:** `stats.ts` обновлён (197 / 17 / 28); у дексаметазона и бетаметазона проставлена стереохимия (теперь разные SMILES — эпимеры C16). Закоммичено локально.
> - **2026-09-19 (чистка):** удалены 2 точных дубля-записи (`drug-199-acepromazine-combo`, `drug-204-marbofloxacin-extra`); добавлен SMILES ацепромазин-малеат (основание); **пересчитаны `hbd/hba/logp` для всех 196 препаратов с SMILES через RDKit** (вместо ручных, расходившихся до 64%); обновлены `stats.ts` (202 / 196 / 17 / 28 / 17) и **README** (17 патогенов + полный список, 196 SMILES, 28 ферментов, бейдж тестов 121→148). Ибафлоксацин оставлен без SMILES (NCI/PubChem отдают битый обрубок C15 вместо C20). **Сборка была сломана**: `lucide-react` не объявлен в `package.json`, хотя импортируется в 6 файлах — добавлен `lucide-react@0.460.0` (версия 1.47.0 битая внутри). `bun run test` (148 тестов) и `bun run build` (24 роута) — зелёные. Закоммичено локально (без пуша).

---

## Общий вердикт

Данные **условно корректны (~70–80% пригодны)**, но есть три системные проблемы:

1. **Счётчики в `stats.ts` и README устарели** — 3 из 5 проверенных констант не совпадают с реальными массивами.
2. **Поля-дескрипторы (`hbd`, `hba`, `logp`) в `drugs.ts` массово не совпадают с пересчётом через RDKit** (до 64% расхождений у HBA) — видимо, ручной ввод из ненадёжного источника.
3. **26 из 197 SMILES не соответствуют заявленному `mw`** (соли, пептиды, неорганика, смеси).

При этом **все 197 SMILES синтаксически валидны** (RDKit парсит 100%), а **данные мишеней патогенов — в порядке** (26 белков, у всех есть PDB и карманы).

---

## 1. Счётчики (`src/data/stats.ts` + README)

| Константа | Заявлено (старое) | Реально | Статус |
|---|---|---|---|
| `DRUG_COUNT` | 204 | **202** | -2 (удалены точные дубли-записи) |
| `DRUGS_WITH_SMILES` | 190 | **196** | +6 |
| `PATHOGEN_COUNT` | 12 | **17** | +5 |
| `RESTRICTION_ENZYME_COUNT` | 30 | **28** | -2 |
| `TOOL_COUNT` | 17 | 17 | OK |

**Вывод:** `stats.ts` и README обновлены (2026-09-19): теперь 17 патогенов + полный список, 196 SMILES, 28 ферментов, бейдж тестов 148.

**17 патогенов:** `asfv, fmdv, brucella, hpai, newcastle, rabies, salmonella, bvdv, leptospira, pedv, ecoli-vet, anaplasma, clostridium, campylobacter, mycoplasma, h5n1, corynebacterium`.
**`hpai` и `h5n1` — НЕ дубль:** это разные записи (HPAI — общая, H5N1 — подтип со своим HA-таргетом). Оба осмысленны, оставлены.

---

## 2. Химия SMILES (RDKit)

- **196** препаратов с SMILES, **0** невалидных (все парсятся `Chem.MolFromSmiles`). OK
- **6** препаратов **без SMILES** → в структурных инструментах не участвуют (корректно, это биологики/комплексы):
  `Ибафлоксацин` (малая молекула, но NCI/PubChem отдают битый обрубок C15 вместо C20 — оставлен пустым намеренно), `Интерферон альфа`, `Интерферон гамма`, `Тимозин альфа`, `Пэг-интерферон`, `Железа декстран`.

---

## 3. `mw` против SMILES — 26 расхождений (>2 Да или >2%)

> **Уточнение (2026-09-19):** внешние чат-боты подтвердили, что часть SMILES «совсем не те». Глубокая проверка RDKit показала: это не просто «нет соли/гидрата», а **РЕАЛЬНО неверные SMILES**. Беназеприл и селенит натрия — откровенный мусор (`C[SiH3]` метилсилан / `[Se]` атом); сукральфат — вообще другая молекула; абамектин/спиносад — димеры (2 фрагмента через `.`); полимиксин B — обрубок пептида; дельтаметрин — неполная/без-Br.
>
> **✅ ВСЕ 7 (выделенные МУСОР) ИСПРАВЛЕНЫ 2026-09-19** — заменены на корректные структуры из NCI/CACTUS + PubChem, верифицированы через RDKit (формулы совпали, все парсятся как единые молекулы; селенит натрия — корректная соль `Na2SeO3`, 3 фрагмента: 2×`[Na+]` + `[O-][Se](=O)[O-]`). Пункты «Медь сульфат» и «Прокаин-пенициллин» остаются «на половину» (категория «нет противоиона/соли», не критично).

**Категория А — SMILES «недобирает» молекулу** (соль/пептид/неорганика/смесь); заявленный `mw` обычно правильный, а SMILES — нет:

| Препарат | RDKit по SMILES (было) | Заявлено `mw` | Суть | Статус |
|---|---|---|---|---|
| Сукральфат | 324.45 | 2086 | SMILES C19H20N2OS — не sucrose и не sucralfate (другая молекула) | ✅ исправлено (сульфат сахарозы C12H22O35S8) |
| Абамектин | 1732.15 | 873.1 | SMILES — димер, 2 фрагмента через `.` (реальный — мономер B1a) | ✅ исправлено (мономер C48H72O14) |
| Спиносад | 1477.96 | 732 | SMILES — димер, 2 фрагмента через `.` (реальный — спинозин A) | ✅ исправлено (мономер C41H65NO10) |
| Полимиксин B | 550.47 | 1200 | SMILES — обрубок C24 вместо циклического декапептида | ✅ исправлено (декапептид B1 C56H98N16O13) |
| Беназеприл | 46.14 | 424.5 | SMILES = `C[SiH3]` (метилсилан) — не беназеприл | ✅ исправлено (ACE-ингибитор C24H28N2O5) |
| Дельтаметрин | 297.74 | 505.2 | SMILES C17H12ClNO2 без Br — неполная/не та структура | ✅ исправлено (пиретроид C22H19Br2NO3) |
| Селенит натрия | 78.96 | 173 | SMILES = `[Se]` (один атом селена) | ✅ исправлено (соль Na2SeO3) |
| Медь сульфат | 159.61 | 249.7 | `CuSO4` верен; stored MW — пентагидрат (не хватает H2O) | «на половину» — не чинил |
| Прокаин-пенициллин | 334.4 | 588.7 | активная часть (пенициллин G) верна, нет прокаина | «на половину» — не чинил |

**Категория Б — умеренные (8–22%, форма соли/гидрата/ионизации):** Тилдипирозин, Гомидий, Изометамидий, Лотиланер, Сароланер, Диклазурил, Эпсипрантел, Салиномицин, Сульфагуанидин.

**Категория В — мелкие (<3%, округление/изотопы):** Альфа-токоферол, Эприномектин, Бакилоприм, Цефкином, Маропитант, Мавакоксиб, Поназурил.

---

## 4. Дескрипторы `hbd` / `hba` / `logp` — ✅ ИСПРАВЛЕНО (2026-09-19)

| Поле | Расхождений с RDKit (было) | Доля |
|---|---|---|
| `hbd` | 73 / 197 | 37% |
| `hba` | 127 / 197 | 64% |
| `logp` (|>1.0|) | 70 / 197 | 35% |

Закономерность: заявленные значения **системно недосчитывали** HBA/HBD для крупных полярных/заряженных молекул (аминогликозиды: неомицин HBA 14 vs RDKit 19, HBD 10 vs 12; эритромицин 9 vs 14), а `logp` для заряженных соединений сильно завышен (неомицин заявлен -3 vs RDKit -8.9).

**✅ Решено:** все 196 препаратов с SMILES получили `hbd/hba/logp`, пересчитанные через RDKit (NumHDonors / NumHAcceptors / MolLogP). Ручные значения заменены. Для 6 препаратов без SMILES поля оставлены как есть.

---

## 5. Дубликаты SMILES — 1 оставшаяся группа

1. **Бензилпенициллин / Прокаин-пенициллин** — одно ядро пенициллина G; соль прокаина не закодирована (избыточность формы соли, не ошибка). — **оставлено**.
2. ~~**Марбофлоксацин / Марбофлоксацин+ (доп.)** — полный дубль записи.~~ **УДАЛЕНО (2026-09-19):** `drug-204-marbofloxacin-extra` (точный дубль, опечатка в имени).
3. **Дексаметазон / Бетаметазон** — **ИСПРАВЛЕНО (2026-09-17):** проставлена стереохимия, теперь разные SMILES (эпимеры по C16).
4. ~~**Ацепромазин / Ацепромазин (комбо)** — полный дубль записи.~~ **УДАЛЕНО (2026-09-19):** `drug-199-acepromazine-combo` (точный дубль ацепромазина). `drug-191-acepromazine-maleate` сохранён (малеат — отдельная соль, добавлен SMILES основания).

`name` и `inn` дублей **нет** (0) — идентификаторы уникальны. OK

---

## 6. Патогены и мишени (докинг-датасет) — в порядке

- 17 патогенов, **26 целевых белков** (`targets`), у **100%** есть `pdb_id` и `pockets`.
- Ранее видел «targets=0» — это был баг моего экстрактора (читал несуществующее поле `proteins`); данные на самом деле заполнены.
- Единственное замечание: `hpai` vs `h5n1` (см. раздел 1).

---

## Что чинить (приоритет)

1. ~~**`stats.ts` + README:** обновить счётчики и список патогенов.~~ **✅ СДЕЛАНО (2026-09-19):** `stats.ts` = 202/196/17/28/17; README — 17 патогенов + полный список, 196 SMILES, 28 ферментов, бейдж 148.
2. ~~**Дексаметазон/Бетаметазон:** проставить стереохимию (`@`).~~ **СДЕЛАНО (2026-09-17).**
3. ~~**СЛОМАННЫЕ SMILES (раздел 3, помечено МУСОР).**~~ **✅ СДЕЛАНО (2026-09-19).**
4. ~~**Дескрипторы `hbd/hba/logp`:** пересчитать из SMILES (RDKit).~~ **✅ СДЕЛАНО (2026-09-19):** все 196 с SMILES пересчитаны (NumHDonors/NumHAcceptors/MolLogP).
5. **Расхождения `mw` (остаток раздела 3):** после фикса 7 МУСОР-позиций большинство `mw` совпадает с SMILES. Оставшиеся ~19 — соли/гидраты/пептиды (сукральфат 2086 = алюминиевый комплекс, медь сульфат = пентагидрат, прокаин-пенициллин = соль прокаина). `mw`, видимо, по лекарственной форме, SMILES — по активному началу. **Не трогал** — менять семантику `mw` рискованно; при желании добавить `mw_freebase` из SMILES.
6. **6 препаратов без SMILES:** интерфероны/тимозин/железа-декстран — биологики/комплексы (корректно исключены из структурных инструментов). `Ибафлоксацин` — малая молекула, но надёжного SMILES нет (NCI/PubChem отдают битый C15-обрубок); оставлен пустым намеренно.

## Что НЕ проверялось

- `TOOL_COUNT=17` (23 роута — надо сверить, какие страницы считаются «инструментами»).
- Корректность самих PDB-карманов (`cx/cy/cz/radius`) — проверялась только наличие, не геометрия.
- Данные по молекулярным часам (README: «12 molecular clock rates») — отдельный источник.
